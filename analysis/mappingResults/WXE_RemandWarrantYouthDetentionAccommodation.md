# WXE RemandWarrantYouthDetentionAccommodation

- **Template ID:** `WXE_RemandWarrantYouthDetentionAccommodation`
- **Source file:** `analysis/templates/WXE_RemandWarrantYouthDetentionAccommodationTemplate.docx`
- **Type:** Warrant
- **Delivery:** File storage
- **Language:** English
- **Code:** `WXE`
- **Field count:** 43

## Merge fields

| Field | Category | Source kind | Hearing-results source | API Response available | API Response note |
|---|---|---|---|---|---|
| `additionalBailExceptionReasons` | prompt (value) | Judicial result prompt | judicialResultPrompts[promptReference ≈ "additionalBailExceptionReasons"].value / .label (matched case-insensitively) | Partial | no stable promptReference key exists here — would have to fuzzy-match offences[].results[].resultTexts[].label against a hardcoded display string for "additionalBailExceptionReasons", then read .value |
| `amendmentDate` | date field | Hearing-resulted event | built by the NowContent mapper layer (cpp-context-azure-legalaidagency) from hearing/court-centre/offence data — exact path not traced field-by-field | Partial | not individually verified against this schema — likely resolvable if it names a defendant/offence/court field already listed as available |
| `bailException` | prompt (value) | Judicial result prompt | judicialResultPrompts[promptReference ≈ "bailException"].value / .label (matched case-insensitively) | Partial | no stable promptReference key exists here — would have to fuzzy-match offences[].results[].resultTexts[].label against a hardcoded display string for "bailException", then read .value |
| `bailExceptionReason` | prompt (value) | Judicial result prompt | judicialResultPrompts[promptReference ≈ "bailExceptionReason"].value / .label (matched case-insensitively) | Partial | no stable promptReference key exists here — would have to fuzzy-match offences[].results[].resultTexts[].label against a hardcoded display string for "bailExceptionReason", then read .value |
| `conveyorcustodiannameOrganisationName` | prompt (value) | Judicial result prompt | judicialResultPrompts[promptReference ≈ "conveyorcustodiannameOrganisationName"].value / .label (matched case-insensitively) | Partial | no stable promptReference key exists here — would have to fuzzy-match offences[].results[].resultTexts[].label against a hardcoded display string for "conveyorcustodiannameOrganisationName", then read .value |
| `convictionStatus` | field | Hearing-resulted event | derived: offence.convictionDate present → "Convicted"/"Not Convicted" | Yes | derivable: offences[].convictionDate present → "Convicted"/"Not Convicted" |
| `dateToBeFixed` | prompt (label) | Judicial result prompt | judicialResultPrompts[promptReference ≈ "dateToBeFixed"].value / .label (matched case-insensitively) | Partial | no stable promptReference key exists here — would have to fuzzy-match offences[].results[].resultTexts[].label against a hardcoded display string for "dateToBeFixed", then read .value |
| `defendant.address.line1` | field | Hearing-resulted event | personDefendant.personDetails.address.address{1-5}/postcode | Yes | defendant.address.address{1-5} / .postCode |
| `defendant.address.line2` | optional field | Hearing-resulted event | personDefendant.personDetails.address.address{1-5}/postcode | Yes | defendant.address.address{1-5} / .postCode |
| `defendant.address.line3` | optional field | Hearing-resulted event | personDefendant.personDetails.address.address{1-5}/postcode | Yes | defendant.address.address{1-5} / .postCode |
| `defendant.address.line4` | optional field | Hearing-resulted event | personDefendant.personDetails.address.address{1-5}/postcode | Yes | defendant.address.address{1-5} / .postCode |
| `defendant.address.line5` | optional field | Hearing-resulted event | personDefendant.personDetails.address.address{1-5}/postcode | Yes | defendant.address.address{1-5} / .postCode |
| `defendant.address.postCode` | field | Hearing-resulted event | personDefendant.personDetails.address.address{1-5}/postcode | Yes | defendant.address.address{1-5} / .postCode |
| `defendant.dateOfBirth` | date field | Hearing-resulted event | personDefendant.personDetails.dateOfBirth | Yes | defendant.dateOfBirth |
| `defendant.name` | field | Hearing-resulted event | personDefendant.personDetails.firstName + lastName (or legalEntityDefendant.organisation.name) | Yes | defendant.title + firstName + middleName + lastName |
| `defendantConsentsNotToBeProducedUntil` | prompt (dateValue) | Judicial result prompt | judicialResultPrompts[promptReference ≈ "defendantConsentsNotToBeProducedUntil"].value / .label (matched case-insensitively) | Partial | no stable promptReference key exists here — would have to fuzzy-match offences[].results[].resultTexts[].label against a hardcoded display string for "defendantConsentsNotToBeProducedUntil", then read .value |
| `designatedLocalAuthority` | prompt (value) | Judicial result prompt | judicialResultPrompts[promptReference ≈ "designatedLocalAuthority"].value / .label (matched case-insensitively) | Partial | no stable promptReference key exists here — would have to fuzzy-match offences[].results[].resultTexts[].label against a hardcoded display string for "designatedLocalAuthority", then read .value |
| `fixedDate` | prompt (dateValue) | Judicial result prompt | judicialResultPrompts[promptReference ≈ "fixedDate"].value / .label (matched case-insensitively) | Partial | no stable promptReference key exists here — would have to fuzzy-match offences[].results[].resultTexts[].label against a hardcoded display string for "fixedDate", then read .value |
| `hCHOUSEAddress1` | prompt (value) | Judicial result prompt | judicialResultPrompts[promptReference ≈ "hCHOUSEAddress1"].value / .label (matched case-insensitively) | Partial | no stable promptReference key exists here — would have to fuzzy-match offences[].results[].resultTexts[].label against a hardcoded display string for "hCHOUSEAddress1", then read .value |
| `hCHOUSEAddress2` | prompt (value) | Judicial result prompt | judicialResultPrompts[promptReference ≈ "hCHOUSEAddress2"].value / .label (matched case-insensitively) | Partial | no stable promptReference key exists here — would have to fuzzy-match offences[].results[].resultTexts[].label against a hardcoded display string for "hCHOUSEAddress2", then read .value |
| `hCHOUSEAddress3` | prompt (value) | Judicial result prompt | judicialResultPrompts[promptReference ≈ "hCHOUSEAddress3"].value / .label (matched case-insensitively) | Partial | no stable promptReference key exists here — would have to fuzzy-match offences[].results[].resultTexts[].label against a hardcoded display string for "hCHOUSEAddress3", then read .value |
| `hCHOUSEAddress4` | prompt (value) | Judicial result prompt | judicialResultPrompts[promptReference ≈ "hCHOUSEAddress4"].value / .label (matched case-insensitively) | Partial | no stable promptReference key exists here — would have to fuzzy-match offences[].results[].resultTexts[].label against a hardcoded display string for "hCHOUSEAddress4", then read .value |
| `hCHOUSEAddress5` | prompt (value) | Judicial result prompt | judicialResultPrompts[promptReference ≈ "hCHOUSEAddress5"].value / .label (matched case-insensitively) | Partial | no stable promptReference key exists here — would have to fuzzy-match offences[].results[].resultTexts[].label against a hardcoded display string for "hCHOUSEAddress5", then read .value |
| `hCHOUSEOrganisationName` | prompt (value) | Judicial result prompt | judicialResultPrompts[promptReference ≈ "hCHOUSEOrganisationName"].value / .label (matched case-insensitively) | Partial | no stable promptReference key exists here — would have to fuzzy-match offences[].results[].resultTexts[].label against a hardcoded display string for "hCHOUSEOrganisationName", then read .value |
| `hCHOUSEPostCode` | prompt (value) | Judicial result prompt | judicialResultPrompts[promptReference ≈ "hCHOUSEPostCode"].value / .label (matched case-insensitively) | Partial | no stable promptReference key exists here — would have to fuzzy-match offences[].results[].resultTexts[].label against a hardcoded display string for "hCHOUSEPostCode", then read .value |
| `hCROOM` | prompt (value) | Judicial result prompt | judicialResultPrompts[promptReference ≈ "hCROOM"].value / .label (matched case-insensitively) | Partial | no stable promptReference key exists here — would have to fuzzy-match offences[].results[].resultTexts[].label against a hardcoded display string for "hCROOM", then read .value |
| `HDATE` | prompt (dateValue) | Judicial result prompt | judicialResultPrompts[promptReference ≈ "HDATE"].value / .label (matched case-insensitively) | Partial | no stable promptReference key exists here — would have to fuzzy-match offences[].results[].resultTexts[].label against a hardcoded display string for "HDATE", then read .value |
| `onlyRemandToSuchAccommodationWouldBeAdequateTo` | prompt (value) | Judicial result prompt | judicialResultPrompts[promptReference ≈ "onlyRemandToSuchAccommodationWouldBeAdequateTo"].value / .label (matched case-insensitively) | Partial | no stable promptReference key exists here — would have to fuzzy-match offences[].results[].resultTexts[].label against a hardcoded display string for "onlyRemandToSuchAccommodationWouldBeAdequateTo", then read .value |
| `orderDate` | date field | Hearing-resulted event | derived: the ordered judicial result's orderedDate | No | no order/result date field — closest are hearing.hearingDate / offences[].convictionDate / .pleaDate |
| `orderingCourt.courtCentreName` | field | Hearing-resulted event | resolved court-house lookup (courtCentre.name / convictingCourt.name) | Yes | hearing.courtDetails.court.courtHouseName |
| `orderingCourt.ljaCode` | field | Hearing-resulted event | resolved court-house lookup (courtCentre / offence.convictingCourt).lja.ljaCode | No | Court carries courtHouseId/Code/Name only — no separate LJA code field |
| `orderingCourt.ljaName` | field | Hearing-resulted event | courtCentre.lja.ljaName | Yes | hearing.courtDetails.ljaName |
| `orderName` | field | NOW definition catalogue | NOW definition catalogue's own `now.name` — static per document type | No | NOW catalogue metadata — not part of any hearing/case API response |
| `reasons` | prompt (value) | Judicial result prompt | judicialResultPrompts[promptReference ≈ "reasons"].value / .label (matched case-insensitively) | Partial | no stable promptReference key exists here — would have to fuzzy-match offences[].results[].resultTexts[].label against a hardcoded display string for "reasons", then read .value |
| `reasonsForYouthDetentionRemand` | prompt (value) | Judicial result prompt | judicialResultPrompts[promptReference ≈ "reasonsForYouthDetentionRemand"].value / .label (matched case-insensitively) | Partial | no stable promptReference key exists here — would have to fuzzy-match offences[].results[].resultTexts[].label against a hardcoded display string for "reasonsForYouthDetentionRemand", then read .value |
| `reference` | field | Hearing-resulted event | offence's case: prosecutionCaseIdentifier.caseURN (or .prosecutionAuthorityReference) | Yes | prosecutionCase.caseURN |
| `remandBasis` | prompt (value) | Judicial result prompt | judicialResultPrompts[promptReference ≈ "remandBasis"].value / .label (matched case-insensitively) | Partial | no stable promptReference key exists here — would have to fuzzy-match offences[].results[].resultTexts[].label against a hardcoded display string for "remandBasis", then read .value |
| `timeOfHearing` | prompt (value) | Judicial result prompt | judicialResultPrompts[promptReference ≈ "timeOfHearing"].value / .label (matched case-insensitively) | Partial | no stable promptReference key exists here — would have to fuzzy-match offences[].results[].resultTexts[].label against a hardcoded display string for "timeOfHearing", then read .value |
| `title` | field | Hearing-resulted event | offence.offenceTitle | Yes | offences[].title |
| `toBeKeptInYouthDetentionAccommodationOnTheGroundsThat` | prompt (value) | Judicial result prompt | judicialResultPrompts[promptReference ≈ "toBeKeptInYouthDetentionAccommodationOnTheGroundsThat"].value / .label (matched case-insensitively) | Partial | no stable promptReference key exists here — would have to fuzzy-match offences[].results[].resultTexts[].label against a hardcoded display string for "toBeKeptInYouthDetentionAccommodationOnTheGroundsThat", then read .value |
| `tVLinkPrehearingConferenceTime` | prompt (label) | Judicial result prompt | judicialResultPrompts[promptReference ≈ "tVLinkPrehearingConferenceTime"].value / .label (matched case-insensitively) | Partial | no stable promptReference key exists here — would have to fuzzy-match offences[].results[].resultTexts[].label against a hardcoded display string for "tVLinkPrehearingConferenceTime", then read .value |
| `weekCommencing` | prompt (dateValue) | Judicial result prompt | judicialResultPrompts[promptReference ≈ "weekCommencing"].value / .label (matched case-insensitively) | Partial | no stable promptReference key exists here — would have to fuzzy-match offences[].results[].resultTexts[].label against a hardcoded display string for "weekCommencing", then read .value |
| `wording` | field | Hearing-resulted event | offence.wording + "\n" + offence.offenceLegislation | Yes | offences[].wording + "\n" + offences[].offenceLegislation (kept as two separate fields here) |
